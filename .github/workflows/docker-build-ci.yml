name: Docker Image CI

on:
  push:
    paths:
      - images/**
  pull_request:
    branches: 
      - main

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        
    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker images
      run: |
        cd images
        for folder in *;
        do
        docker build $folder -t $folder:$(date +%s)
        done
        